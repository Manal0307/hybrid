export function markHomeIntroDone() {}

export function shouldSkipHomeIntro() {
  return false;
}

/** Link target — home page (loading + textes + scène bag, comme à la première visite). */
export const homeBagLink = {
  pathname: "/",
};
